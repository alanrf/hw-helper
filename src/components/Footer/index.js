import React from 'react';
import { FooterBase } from './styles';

function Footer() {
  return (
    <FooterBase>
      <p>
        Tool created to help and act like a guide on Hero Wars.
      </p>
    </FooterBase>
  );
}

export default Footer;
